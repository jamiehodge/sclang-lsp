[package]
name = "sclang-syntax"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Lexer and parser for SuperCollider, derived from sclang's own front end"

[dependencies]
